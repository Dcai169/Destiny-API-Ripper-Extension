const assert = require('assert');
const { setVisibility } = require('../src/main/scripts/uiUtils.js');

describe('uiUtils.js', () => {
    describe('setVisibility', () => {
        it('should assign classes correctly.', () => {
        });
    });
});
